# Session Connect — fictive

Fichier machine : [`connect-session.json`](connect-session.json).

Ce n'est **pas** un sceau. Les hash `aaa…` / `bbb…` sont des placeholders.
Un lab copie le JSON pour voir `allow` (sim), `deny` (train), `urgent` (poitrine).

Pour une vraie session : `owner.txt` hors git + Scaniverse + `situs-auto`, puis remplacer les hash.
