# Liens

```
FIGURE          qui
SITUS           où
  ANNUAIRE      fiche publique (empreinte + usages)
      ↓
  CONNECT       autoriser
      ↓ douane (quota / jour / lab)
      ↓ allow → QUITTANCE signée
      ↓ allow+message → Ollama (Filon)
UNFORGE         quoi — sceau .unforge.json
QUELLE          d'où le bit (optionnel, --quelle)
TÉMOIN          force du bit
HORIZON         jusqu'à quand (--horizon)
```

Le splat ne va pas sur GitHub. `quantum.db` non plus.
Pas de canal quantique simulé. Pas de lien MESURE tant que le repo n'existe pas.
